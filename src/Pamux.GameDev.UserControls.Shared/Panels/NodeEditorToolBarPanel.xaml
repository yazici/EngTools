<UserControl x:Class="Pamux.GameDev.UserControls.Panels.NodeEditorToolBarPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:panels="clr-namespace:Pamux.GameDev.UserControls.Panels"
             xmlns:tabs="clr-namespace:Pamux.GameDev.UserControls.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300" >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../SharedResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Button Grid.Row="0" Width="{Binding Mode=OneWay, Source={StaticResource ToolButtonWidth}}"  Height="{Binding Mode=OneWay, Source={StaticResource ToolButtonHeight}}" Content="+" Command="{Binding AddNodeCommand}" CommandParameter="{Binding}"/>
    </Grid>
</UserControl>
